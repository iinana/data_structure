#pragma once

#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>
#include <ctype.h>
#include "../hash/hashFIO.h"

bool TwoSum(Hash **table, int n);
int (*SymmetricPairs(Hash **table, int len))[2];